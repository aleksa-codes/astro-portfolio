---
import { cn } from "@/lib/utils"

interface Props {
  class?: string
}

const { class: className }: Props = Astro.props
---

<span
  class={cn(
    "inline-flex rounded-lg border border-border bg-secondary/30 px-3.5 py-1.5 text-sm font-medium",
    className
  )}
>
  <slot />
</span>
