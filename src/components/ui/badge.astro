---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className }: Props = Astro.props;
---

<span
  class={cn(
    'border-border bg-secondary/30 inline-flex rounded-lg border px-3.5 py-1.5 text-sm font-medium',
    className,
  )}
>
  <slot />
</span>
